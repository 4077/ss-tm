<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <!-- user -->
    <div class="login">{LOGIN}</div>
    <!-- / -->

    <div class="session_key">{SESSION_KEY}</div>
    <div class="ip">{IP}</div>
    <div class="referer">{REFERER}</div>
    <div class="user_agent">{USER_AGENT}</div>
    <div class="device">{DEVICE}</div>

</div>
